server:
  port: 8762
  ## 注册到服务中心的名字
spring:
  application:
    name: nativeconfigserver
## native 代表本地
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared








